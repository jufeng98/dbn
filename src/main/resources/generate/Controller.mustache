package {{controllerPackageTextField}};

import {{modelPackage}}.{{reqFileName}};
import {{modelPackage}}.{{resFileName}};
import {{servicePackage}}.{{serviceFileName}};
import {{resultPackage}}.{{resultFileName}};
import io.swagger.annotations.Api;
import io.swagger.annotations.ApiOperation;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.validation.annotation.Validated;
import org.springframework.web.bind.annotation.CrossOrigin;
import org.springframework.web.bind.annotation.PostMapping;
import org.springframework.web.bind.annotation.RequestBody;
import org.springframework.web.bind.annotation.RequestMapping;
import org.springframework.web.bind.annotation.RestController;
{{#feign}}import {{apiPackageTextField}}.{{feignFileName}};{{/feign}}

@CrossOrigin
@RestController
@RequestMapping("/{{businessNameTextField}}")
@Api(tags = "TODO Auto-generate")
public class {{businessNamePrefix}}{{controllerPostfixTextField}} {{#feign}}implements {{feignFileName}};{{/feign}} {
    @Autowired
    private {{serviceFileName}} {{serviceFieldName}};

    @ApiOperation(value = "TODO Auto-generate")
    @PostMapping("/{{methodNameTextField}}")
    public Result<{{resFileName}}> {{methodNameTextField}}(@Validated @RequestBody {{reqFileName}} reqVo) {
        return Result.success({{serviceFieldName}}.{{methodNameTextField}}(reqVo));
    }

}
